# Build artefacts
build/

# Node / Playwright
node_modules/
playwright-report/
test-results/
.playwright/

# Test secrets and saved auth state
tests/.env
.auth-state.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Deployment packages (binary artefacts, transfer manually)
*.tar.gz
*.tar
oc-pilot-telemetry-deploy/
oc-pilot-mongo-deploy/

# Claude worktrees
.claude/worktrees/

# Server dashboard build output and deps
server/dashboard/dist/
server/dashboard/node_modules/

# Server logs (bind-mounted at runtime)
server/logs/

# Server local config (contains secrets)
server/config.yaml

# Extension telemetry config — lives in src/ for runtime loading by background.js.
# Contains the bearer token. Excluded from CRX builds by pack.ps1 (deleted from
# staging before zip). Never committed; copy src/t.config.example to src/t.config.
src/t.config
