# Build artefacts
build/

# Node / Playwright
node_modules/
playwright-report/
tests/results/
.playwright/

# Exception: extension/node_modules/ IS committed so airgapped machines can
# build the extension without internet access (typescript + esbuild + types).
# See docs/ARCHITECTURE.md → Air-gap build. ~34 MB.
!extension/node_modules/

# Extension build outputs (esbuild writes bundles + sourcemaps here).
extension/dist/
extension/*.tsbuildinfo

# Test secrets and saved auth state
tests/.env
tests/.auth-state.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Deployment packages (binary artefacts, transfer manually)
*.tar.gz
*.tar
oc-pilot-telemetry-deploy/
oc-pilot-mongo-deploy/

# Claude worktrees
.claude/worktrees/

# Telemetry-server dashboard build output and deps
telemetry-server/dashboard/dist/
telemetry-server/dashboard/node_modules/

# Telemetry-server logs (bind-mounted at runtime)
telemetry-server/logs/

# Telemetry-server local config (contains secrets)
telemetry-server/config.yaml

# Extension telemetry config — lives in extension/ for runtime loading by
# background.js. Contains the bearer token. Excluded from CRX builds by
# scripts/build-crx.ps1 (deleted from staging before zip). Never committed;
# copy extension/t.config.example to extension/t.config.
extension/t.config
