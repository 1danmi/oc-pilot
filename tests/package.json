{
  "//": "Self-contained Playwright test runner for the OC Pilot Chrome extension. The extension itself (../extension/) has no JS deps. The telemetry-server (../telemetry-server/) has its own Python + Node setup. Run `npm test` from THIS directory.",
  "name": "oc-pilot-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E test runner for the OC Pilot Chrome extension (../extension/)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "dotenv": "^16.4.5"
  }
}
