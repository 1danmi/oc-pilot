# Copy this file to tests/.env and fill in the values.
# tests/.env is gitignored — never commit credentials.

# OKD console URL (default if unset: http://localhost:9000)
# CONSOLE_URL=http://localhost:9000

# Required: kubeadmin password used by tests/fixtures/auth.setup.ts
KUBEADMIN_PASSWORD=
